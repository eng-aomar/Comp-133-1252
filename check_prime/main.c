#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main()
{
    bool res; // true pr false // bool x=true(nonzero number); or bool y=false(0);
    return 0;
}

bool isPrime(int num)
{
// 13
  //range 2 --- num -1(12)
  // for loop 2 to num -1 // is prime true else false


}
