#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
   // printf("Hello world!\n");
    //double x,y,z,Z;
    int x = 387, reverse; // 783
    int ones = x%10; // + - * / %
    int tens=x%100/10;    // 387 /10 % 10;
    int hands = x/100;
    printf("%d",tens);
    //printf()
  // Z = ( 4.2*(x+y)/z-0.25*x/(y+z))/(pow(x+y,2));
  //Z = ()/((x+y)*(x+y));



    return 0;
}
