#include <stdio.h>

int main() {

    char name[] = "Alaa"; // auto --> complier size \0
    int size=sizeof(name)/sizeof(name[0]); // \0
    printf("Name: %s\n Size %d", name, size);

    return 0;
}
