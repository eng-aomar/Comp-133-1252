#include <stdio.h>

int main() {

    char name[50];

    printf("Enter your name: ");
    //scanf("%s",name); // stops after space
    fgets(name, sizeof(name), stdin);
    puts(name); // Add new line
    puts(name);
    printf("Name: %s", name);
    printf("Name: %s", name);
    return 0;
}
